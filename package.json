{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.123"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o myApiTest",
    "test": "redocly lint",
    "build-docs": "npm run build; redocly build-docs myApiTest.yaml -o myApiTest.html"
  }
}
